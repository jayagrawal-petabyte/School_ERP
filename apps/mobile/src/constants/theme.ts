import { colors, spacing, typography, fonts, shadows } from '../theme';

export const COLORS = colors;
export const SPACING = spacing;
export const FONT_SIZE = typography.sizes;
export const FONT_WEIGHT = fonts.weights;
export const SHADOWS = shadows;
